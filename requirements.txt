# Replicating "Toward Universal Steering and Monitoring of AI Models" on GuppyLM
# Tested on Python 3.14 / macOS (CPU only — no GPU required).
#
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt

# --- Model: forward passes, activation hooks, steering hooks ---
torch>=2.0.0                # GuppyLM inference; forward_hook API for extraction + steering
tokenizers>=0.19.0          # GuppyLM's BPE tokenizer (tokenizer.json)

# --- Numerics: RFM / AGOP, kernels, probes, baselines ---
numpy>=1.24.0               # activations, AGOP matrices, eigendecomposition
scipy>=1.10.0               # cdist for Mahalanobis distances; Pearson correlation
scikit-learn>=1.3.0         # LogisticRegression probes, StandardScaler, AUROC (monitoring + baselines)

# --- Data: concept datasets and the pretrained checkpoint ---
datasets>=2.14.0            # loads arman-bd/guppylm-60k-generic (Phase 2)
huggingface_hub>=0.20.0     # downloads arman-bd/guppylm-9M into checkpoints/
tqdm>=4.65.0                # progress bars (also required by the vendored guppylm/ code)

# --- Optional: only needed for ad-hoc inspection of cached results ---
pandas>=2.0.0
matplotlib>=3.7.0
